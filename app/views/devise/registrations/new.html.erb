<div id="sign-up-block" class="authentication-block">
  <%= render 'layouts/logo_250' %>
  <h4 align="center">Sign Up</h4>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>
    <div class="field form-group">
      <%= f.email_field :email, autofocus: true, :placeholder => "Email", :class => "form-control" %>
    </div>

    <div class="field form-group">
      <%= f.password_field :password, autocomplete: "off", :placeholder => "Password - Minimum 8", :class => "form-control" %>
     <!--  <% if @validatable %>
      <p class="help-block">(<%= @minimum_password_length %> characters minimum)</p>
      <% end %> -->
    </div>

    <div class="field form-group">
      <%= f.password_field :password_confirmation, autocomplete: "off", :placeholder => "Retype Password", :class => "form-control" %>
    </div>

    <div class="actions">
      <%= f.submit "Sign up", :id => "sign-up-btn",
        :class => "btn kariton-btn btn-block" %>
    </div>
  <% end %>
</div>

<div class="authentication-block">
  <p align="center">or <%= link_to "Log in", new_session_path(resource_name) %></p>
</div>
