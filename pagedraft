Pages/Controllers

Authentication
	login
	logout
	viewUser
	authenticate

Cart
	addToCart
	removeFromCart
	checkOutCart
	viewCart

Item
	browseItems (optional parameter: query)


Models/Tables

User
	name (string)
	active_cart (integer)

Cart
	# cartID
	userID (integer)
	checkout_date (date_time)

CartItem
	itemID (integer)
	cartID (integer)
	quantity (integer)
	price (decimal)

Item
	#itemID
	description (text)
	name (string)
	price (decimal)
	stock (integer)